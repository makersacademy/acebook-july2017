 <h1>NewsFeed</h1>

<% @activities.each do |activity| %>
  <div class = "activity">
  [username here]<%= link_to activity.owner.name, activity.owner if activity.owner %>
  <%= activity.trackable_type %>ed <%= link_to activity.trackable.summary, post_path(activity.trackable) %>
</div>
<%end%>
